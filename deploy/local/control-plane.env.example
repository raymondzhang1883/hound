# Copy to the ignored .hound/control-plane.env and replace both values with random local secrets.
HOUND_POSTGRES_PASSWORD=replace-with-at-least-32-random-characters
HOUND_WORKER_KEY=replace-with-at-least-32-random-characters
HOUND_CONTROL_PORT=8090
HOUND_LEASE_DURATION=30s
